name: Auto approve

on:
  pull_request_target:
    branches:
    - develop

jobs:
  auto-approve:
    runs-on: ubuntu-latest
    steps:
    - uses: hmarr/auto-approve-action@v2
      if: github.actor == 'github[bot]' || github.actor == 'github-actions[bot]'
      with:
        github-token: "${{ secrets.DTI_DEVOPS_TOKEN }}"