entity type Converter schema name "CNVRTR" {
    // Effeciency of bi-directional converter (0 to 1)
    eff: !float

    // Minimum conversion rate
    rateMin: !float

    // Maximum conversion rate
    rateMax: !float
}