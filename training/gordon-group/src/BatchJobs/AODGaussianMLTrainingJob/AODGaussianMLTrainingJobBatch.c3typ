/**
* Represents a unit of work (batch) in a 
*/
type AODGaussianMLTrainingJobBatch {
    // The {@GeoSurfaceTimePoint}s that define the targets for the models in this batch
    values: [GeoSurfaceTimePoint]
}