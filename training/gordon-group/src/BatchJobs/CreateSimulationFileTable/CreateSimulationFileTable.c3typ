/**
* Batch job to upsert table with files for each {@link Simulationsample}
*/
type CreateSimulationFileTable extends BatchJob<CreateSimulationFileTable, CreateSimulationFileTableOptions, CreateSimulationFileTableBatch> type key 'CRT_SMLTN_FL_TBL' {
    simulationSamples: ![SimulationSample]

    doStart:        ~ js server
    processBatch:   ~ js server
}